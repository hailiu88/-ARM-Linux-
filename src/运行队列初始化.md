## 运行队列初始化

介于autogroup_init()和atomic_set(&rq-\>nr_iowait,
0)的代码段用于初始化所有CPU的运行队列、完全公平运行队列、实时运行队列及截止日期运行队列，并把各个CPU的运行队列挂接到默认根域（def_root_domain），即把各个CPU运行队列的rd字段设置为def_root_domain的地址，为CPU最终运行任务作最后的准备。

