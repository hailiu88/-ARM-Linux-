
## 调试对象基础结构早期初始化

在 Linux 内核中，debug_objects_early_init() 是
对象生命周期调试基础设施（Debug Objects Infrastructure）
的启动函数。主要任务包括初始化用于存储所有调试对象的、称作调试容器的obj_hash数组，为每个哈希桶建立一个用于同步的raw_spinlock_t锁，建立静态池，将预定义的静态对象链接到空闲列表obj_pool中。通过对这些对象的初始化，可以保证在内存分配器尚未就绪的早期，调试器也能正常工作。
