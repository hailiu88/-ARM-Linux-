## 陷阱初始化

陷阱初始化函数为trap_init(void)，与硬件架构有关，主要用来初始化与陷阱运行有关的硬件及陷阱中断向量等。函数定义在各自的架构目录下，如Arm系统的trap_init()定义在git/arch/arm/kernel/trap.c，powePC的trap_init()定义在git/arch/powerpc/kernel/trap.c里。对arm系统而言，trap_init()函数为空函数。


