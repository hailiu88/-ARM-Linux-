## 任务权重及上下文切换机制设置

函数set_load_weight()用于设置初始任务的调度权重，即设置task_struct 中
sched_entity 的 load字段的值。

mmgrab()的作用是增加内核内存描述符init_mm的引用计数。这确保了即便没有用户进程运行，内核的内存上下文也不会被释放。

当系统进入 空闲状态时，内核不会执行昂贵的
页表缓存（TLB）刷新，不需要进行上下文切换。这时可以借用上一个运行进程的页表，从而降低上下文切换开销。这种机制被称为
Lazy
TLB。enter_lazy_tlb()函数通知处理器，现在处于内核上下文，不需要切换用户页表。
