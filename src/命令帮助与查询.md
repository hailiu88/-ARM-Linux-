
## 命令帮助与查询

可以通过如下形式获取命令帮助：

    command --help

或

    command -h（并不是所有命令都支持 -h，有时需要采用`--help`选项）

如：

    $ ls --help

也可以通过命令man获取完整的命令使用说明，形式为：

    man command

如：

    $ man ls

Linux具有非常丰富的命令集，初步估计有两百条以上。在后面几节我们将介绍一些常用的命令。

